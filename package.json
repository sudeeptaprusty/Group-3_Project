{
  "name": "mutual-fund-platform-root",
  "private": true,
  "version": "1.0.0",
  "description": "Orchestrator for decoupled N-Tier Mutual Fund Platform",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "clean": "npm run clean --prefix backend && npm run clean --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.4.2"
  }
}
